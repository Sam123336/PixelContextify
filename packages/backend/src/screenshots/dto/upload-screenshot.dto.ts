/**
 * Placeholder DTO for the upload endpoint. The screenshot itself arrives as a
 * multipart `file` field (validated by Multer + the controller). Additional
 * optional metadata fields can be added here as the product evolves.
 */
export class UploadScreenshotDto {}
