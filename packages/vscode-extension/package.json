{
  "name": "@contextify/vscode-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Contextify VS Code extension (Phase 3 placeholder)",
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests\" && exit 0",
    "lint": "echo \"no lint\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
